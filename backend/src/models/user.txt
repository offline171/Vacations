// User model
// TODO: Define user schema and validation
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

Consider getting rid of email or properly implementing it
How to properly implement it:
https://www.passportjs.org/tutorials/email/

Decided not necessary, accounts aren't particularly important enough. Also costs money
If needed then implement log in with google which is more reasonably priced